enum FeatureTab {
  /** The logged in user's details and other options */
  USER = 'user',
  /** The most recent chats of the logged in user with users or groups */
  RECENT_CHATS = 'recent_chats',
  /** All the groups the logged in  user is a part of or has created */
  GROUPS = 'groups',
  /** All the users with which the logged in user is in contact with */
  CONTACTS = 'contacts'
  // BROADCAST = 'broadcast'
}

export default FeatureTab;
